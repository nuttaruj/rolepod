<!-- rolepod: in-repo GEMINI.md kept as a thin stub.

Rolepod's Gemini always-on core ships inside the extension at
~/.gemini/extensions/rolepod/GEMINI.md, auto-loaded via the extension
manifest's contextFileName. It is a build artifact rendered from
adapters/gemini/GEMINI.md.tmpl + core/fragments/ by build/render.sh.

Gemini also auto-loads a repo-root GEMINI.md (project scope). A full core
here would load on top of the extension's context file — the same block
twice on every Gemini session in this repo. This stub avoids that, matching
the in-repo CLAUDE.md stub.

Install rolepod globally (./install.sh --target=gemini) to dogfood the gates
while working here; run `make render` to refresh the build artifact.
-->
