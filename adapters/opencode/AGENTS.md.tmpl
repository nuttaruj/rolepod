# Rolepod — opencode always-on judgment

Always-on guidance for the opencode Lead. opencode auto-loads this from
`~/.config/opencode/AGENTS.md` (global) or `<repo>/AGENTS.md` (project)
every session. This is judgment — the phase procedure (spec, plan, build,
verify, review, ship) lives in the skills; invoke them.

## Rule priority

1. User instruction this turn
2. Project `<repo>/AGENTS.md`
3. Global `~/.config/opencode/AGENTS.md`
4. This core
5. Default best practice

A conflict that risks harm → ask before acting.

{{INCLUDE: core/fragments/route-first.md}}

{{INCLUDE: core/fragments/identity.md}}

{{INCLUDE: core/fragments/verify-first.md}}

{{INCLUDE: core/fragments/decision-protocol.md}}

{{INCLUDE: core/fragments/code-search.md}}

{{INCLUDE: core/fragments/communication.md}}

{{INCLUDE: core/fragments/risky-actions.md}}

{{INCLUDE: core/fragments/hard-stops.md}}

## opencode specifics

- **Skills and agents** — the rolepod skills install at
  `~/.config/opencode/skills/<name>/` and auto-trigger from their
  `description:`; 15 specialists install at `~/.config/opencode/agents/*.md`
  and run by description or `@<name>`, each carrying its own protocol.
- **Tier classes** — map cheap / balanced / strong ONCE per session onto the
  models the user has opted into (configured providers), never the whole
  aggregator catalog; a rung costlier than anything configured is a cost
  decision to surface. Cheap = small / fast, balanced = mid flagship,
  strong = the set's top reasoning model; the ONE judge or security
  reviewer runs strong, never the whole fan-out. Cannot classify →
  balanced, and say so.
- **Enforcement** — the `plugins/rolepod.js` plugin denies a `git commit`
  with `docs/rolepod/` staged (unless `.rolepod/docs-tracked`), and every
  agent's `permission:` block denies a sub-agent commit / push / merge.
  Review / test evidence gating is Claude-only. Every other rule is
  skill-enforced — never report it as mechanically enforced.
