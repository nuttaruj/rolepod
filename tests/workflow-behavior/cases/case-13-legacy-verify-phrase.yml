name: case-13-legacy-verify-phrase
prompt: |
  Show me the test pass output that proves this change works.
expected_skills:
  - using-rolepod
  - check-work
must_not_contain:
  - "looks fine"
  - "I'm confident it works"
why: |
  Old trigger phrasing for `post-change-verify` (show test pass output,
  verify change). There is no `post-change-verify` skill file anymore; this
  proves the canonical Core 10 route catches the legacy phrasing directly.
